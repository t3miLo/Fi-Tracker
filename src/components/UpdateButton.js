import React from "react";

const UpdateButton = props => {
  
  return(
    <div>
      <button type='submit' className='btn  btn-info btn-sm text-dark mb-2'>Update</button>
    </div>
  )
};

export default UpdateButton;
